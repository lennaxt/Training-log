name: Schlagzeilen holen

on:
  schedule:
    # Alle sechs Stunden reicht: eine Trainingspause braucht keine frische Minute.
    # Nicht zur vollen Stunde, da draengen sich die Laeufe aller Repos.
    - cron: "13 3,9,15,21 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: news
  cancel-in-progress: false

jobs:
  news:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fetch_news.py
      - name: Committen, wenn sich etwas geändert hat
        run: |
          if [ -z "$(git status --porcelain news)" ]; then
            echo "Keine neuen Schlagzeilen."
            exit 0
          fi
          git config user.name "news-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add news
          git commit -m "Schlagzeilen aktualisiert"
          git push
